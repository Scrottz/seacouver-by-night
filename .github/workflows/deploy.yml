name: Deploy Vampire Site

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Aktualisiert von v3 auf v4

      - name: Set up Python
        uses: actions/setup-python@v5 # Aktualisiert von v4 auf v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install mkdocs mkdocs-material openai

      - name: Run AI and Gallery Generator
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python scripts/build_site.py

      - name: Build Static Site
        run: mkdocs build

      - name: Deploy to Server via SCP
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: "site/*" 
          target: "/www/htdocs/w016db5f/seacouver-by-night.de/"
          strip_components: 1
